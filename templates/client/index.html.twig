{% extends 'base.html.twig' %}

{% block title %}Liste des clients{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="page-wrapper">
    <div class="table-responsive">
    <div class="container-fluid">
    <h2>Liste des clients</h2>
    <table class="table table-hover ">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Logo</th>
                <th></th>
                <th>Actions</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for client in clients %}
            <tr>
                <td>{{ client.name }}</td>
                <td>{{ client.logo }}</td>
                <td><a class="btn btn-info" href="{{ path('client_show', {'id': client.id}) }}">Détails</a></td>
                <td><a class="btn btn-warning" href="{{ path('client_edit', {'id': client.id}) }}">Modifier</a></td>
                <td>{{ include('client/_delete_form.html.twig') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        <br>
        <div class="col-12">
            <a class="btn btn-success" href="{{ path('client_new') }}">Ajouter un nouveau client</a>
        </div>
    </div>
    </div>
    </div>
{% endblock %}